---

---

<script>
    import mermaid from "mermaid";

    // Initialize mermaid with default configuration
    mermaid.initialize({
        startOnLoad: true,
        theme: "default",
        securityLevel: "loose",
        fontFamily: "arial,sans-serif",
    });
</script>

